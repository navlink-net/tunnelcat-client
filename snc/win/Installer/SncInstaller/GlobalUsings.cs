// ImplicitUsings doesn't reliably pull in System.IO for a UseWPF=true project on this SDK, so
// declare the commonly-needed ones explicitly here instead of repeating them in every file.
global using System.IO;
